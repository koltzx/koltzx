name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Generate Snake Animation
        uses: Platane/snk@master
        with:
          github_user_name: koltzx
          outputs: |
            dist/github-contribution-grid-snake.svg